---
phase: 05-skills-use-cases
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/sections/useCaseData.ts
  - components/sections/UseCasesSection.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Use case section displays 6 cards with title, subtitle (persona), description, and icon"
    - "Use case cards animate in with fade and stagger as section enters viewport"
    - "Animations respect prefers-reduced-motion"
    - "Both SkillsSection and UseCasesSection are integrated into the homepage replacing placeholder sections"
    - "Page renders Skills section on stone-100 background followed by Use Cases on stone-950 background"
  artifacts:
    - path: "components/sections/useCaseData.ts"
      provides: "Typed UseCase interface and useCases array with 6 items"
      contains: "export interface UseCase"
      exports: ["UseCase", "useCases"]
    - path: "components/sections/UseCasesSection.tsx"
      provides: "Client component rendering use case card grid with ScrollTrigger.batch stagger"
      min_lines: 50
    - path: "app/page.tsx"
      provides: "Homepage with SkillsSection and UseCasesSection imports replacing inline placeholder sections"
      contains: "SkillsSection"
  key_links:
    - from: "components/sections/UseCasesSection.tsx"
      to: "components/sections/useCaseData.ts"
      via: "named imports"
      pattern: "import.*useCases.*useCaseData"
    - from: "components/sections/UseCasesSection.tsx"
      to: "components/ui/Card.tsx"
      via: "Card component with theme='dark' hover"
      pattern: "Card.*theme.*dark.*hover"
    - from: "components/sections/UseCasesSection.tsx"
      to: "ScrollTrigger.batch"
      via: "useGSAP hook with batch('.use-case-card')"
      pattern: "ScrollTrigger\\.batch.*use-case-card"
    - from: "app/page.tsx"
      to: "components/sections/SkillsSection.tsx"
      via: "import and JSX element"
      pattern: "import SkillsSection"
    - from: "app/page.tsx"
      to: "components/sections/UseCasesSection.tsx"
      via: "import and JSX element"
      pattern: "import UseCasesSection"
---

<objective>
Create the Use Cases section with dark-themed card grid and scroll-triggered stagger reveals, then integrate both Skills and Use Cases sections into the homepage.

Purpose: The Use Cases section illustrates who benefits from Slider across 6 personas (Sales Teams, Marketing, Executives, Consultants, Educators, Founders). This plan also wires both Phase 5 sections into page.tsx, replacing the placeholder card grids.
Output: useCaseData.ts data module, UseCasesSection.tsx client component, and updated page.tsx with both new section imports.
</objective>

<execution_context>
@C:\Users\antoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\antoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skills-use-cases/05-RESEARCH.md
@.planning/phases/05-skills-use-cases/05-01-SUMMARY.md

@components/sections/featureData.ts
@components/sections/ProductDemo.tsx
@components/ui/Card.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCaseData.ts and UseCasesSection.tsx</name>
  <files>
    components/sections/useCaseData.ts
    components/sections/UseCasesSection.tsx
  </files>
  <action>
**useCaseData.ts** -- Pure TypeScript data module (no React imports):

Define and export:
1. `UseCase` interface: `id: string`, `title: string`, `subtitle: string`, `description: string`, `iconPaths: string[]`
2. `useCases: UseCase[]` array with 6 items. Use the exact content from RESEARCH.md Example 4:
   - sales-teams: "Win More Deals" / "For Sales Teams"
   - marketing-teams: "Showcase Results" / "For Marketing"
   - executives: "Present with Confidence" / "For Executives"
   - consultants: "Deliver Insights" / "For Consultants"
   - educators: "Engage Students" / "For Educators"
   - founders: "Secure Funding" / "For Founders"

Use the exact SVG iconPaths from RESEARCH.md Example 4. No default export.

**UseCasesSection.tsx** -- Client component ('use client') following SkillsSection pattern:

Imports:
- useRef from react
- useGSAP from @gsap/react
- gsap from gsap
- ScrollTrigger from gsap/ScrollTrigger
- Card from @/components/ui/Card
- { useCases } from @/components/sections/useCaseData

Register ScrollTrigger: `gsap.registerPlugin(ScrollTrigger)`

Component structure:
- `containerRef = useRef<HTMLElement>(null)`
- `headingRef = useRef<HTMLDivElement>(null)`

useGSAP hook (scope: containerRef):
Inside gsap.matchMedia():

`"(prefers-reduced-motion: no-preference)"` branch:
1. Heading fade-in: `gsap.from(headingRef.current, { autoAlpha: 0, y: 40, duration: 0.8, ease: 'power2.out', scrollTrigger: { trigger: headingRef.current, start: 'top 85%', toggleActions: 'play none none none' } })`
2. Batch stagger: `ScrollTrigger.batch('.use-case-card', { onEnter: (elements) => { gsap.from(elements, { autoAlpha: 0, y: 60, duration: 0.8, stagger: 0.15, ease: 'power2.out' }) }, start: 'top 85%', once: true })`

`"(prefers-reduced-motion: reduce)"` branch:
- `gsap.set('.use-case-card', { clearProps: 'all' })`

Return `() => mm.revert()`.

JSX structure (dark theme, flat grid -- no category grouping):
```
<section ref={containerRef} id="use-cases" className="min-h-screen bg-stone-950 text-white py-20 sm:py-24 lg:py-32">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div ref={headingRef} className="text-center mb-16">
      <h2 className="text-4xl sm:text-5xl font-bold mb-4">Built for Every Team</h2>
      <p className="text-stone-400 text-lg max-w-2xl mx-auto">No matter your role, Slider helps you create presentations that make an impact.</p>
    </div>

    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {useCases.map(useCase => (
        <Card key={useCase.id} theme="dark" hover className="use-case-card">
          <div className="space-y-4">
            <div className="w-12 h-12 rounded-lg bg-burnt-orange/10 flex items-center justify-center">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" className="w-6 h-6 text-burnt-orange">
                {useCase.iconPaths.map((d, i) => <path key={i} d={d} />)}
              </svg>
            </div>
            <div>
              <p className="text-burnt-orange text-xs uppercase tracking-wide font-medium mb-2">{useCase.subtitle}</p>
              <h3 className="text-xl font-bold text-white mb-3">{useCase.title}</h3>
              <p className="text-stone-400 text-sm leading-relaxed">{useCase.description}</p>
            </div>
          </div>
        </Card>
      ))}
    </div>
  </div>
</section>
```

Export as default: `export default function UseCasesSection()`

**Important:** Same as SkillsSection -- do NOT add CSS opacity/visibility on use-case-card elements. GSAP autoAlpha handles initial state. Also use a DIFFERENT class selector than Skills (.use-case-card vs .skill-card) to prevent batch overlap.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm TypeScript compiles. Verify useCaseData.ts has 6 use cases with valid iconPaths. Verify UseCasesSection.tsx uses ScrollTrigger.batch('.use-case-card') with once: true.</verify>
  <done>useCaseData.ts exports UseCase type and 6 use cases. UseCasesSection.tsx renders dark-themed card grid with heading fade-in, ScrollTrigger.batch stagger, and reduced motion fallback. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SkillsSection and UseCasesSection into page.tsx</name>
  <files>app/page.tsx</files>
  <action>
Update app/page.tsx to replace the inline placeholder Skills and Use Cases sections with the new imported components.

1. Add imports at top:
   ```
   import SkillsSection from '@/components/sections/SkillsSection'
   import UseCasesSection from '@/components/sections/UseCasesSection'
   ```

2. Replace the entire `{/* Skills Section */}` block (the inline `<section id="skills" ...>...</section>` with 4 hardcoded Card components) with:
   ```
   <SkillsSection />
   ```

3. Replace the entire `{/* Use Cases Section */}` block (the inline `<section id="use-cases" ...>...</section>` with 3 hardcoded Card components) with:
   ```
   <UseCasesSection />
   ```

4. Remove the `Card` import from page.tsx since it will no longer be used directly (Card is only used inside SkillsSection and UseCasesSection now). Also remove `Button` import if no longer used -- check the CTA section first. The CTA section uses `<Button>` so keep the Button import.

The final page.tsx should have this component order:
```
<Hero />
<FeatureShowcase />
<ProductDemo />
<SkillsSection />
<UseCasesSection />
{/* CTA Section (inline) */}
```
  </action>
  <verify>Run `npm run build` to confirm full build succeeds with no errors. Run `npm run dev` and verify the page loads with Skills section (light bg, 9 cards in 3 groups) followed by Use Cases (dark bg, 6 cards). Verify no unused import warnings.</verify>
  <done>page.tsx imports and renders SkillsSection and UseCasesSection, replacing placeholder content. Build passes. Page renders correct section order with proper backgrounds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. useCaseData.ts has 6 use cases, all with valid iconPaths
4. UseCasesSection.tsx uses ScrollTrigger.batch('.use-case-card') with once: true
5. UseCasesSection.tsx uses gsap.matchMedia() with reduced motion fallback
6. page.tsx imports both SkillsSection and UseCasesSection
7. page.tsx no longer contains inline placeholder Skills/Use Cases sections
8. Skills section renders on stone-100 (light), Use Cases on stone-950 (dark)
9. Cards in both sections have hover interactions
10. Both sections have heading fade-in + card batch stagger animations
</verification>

<success_criteria>
- useCaseData.ts exports UseCase type and 6 use cases with persona subtitles
- UseCasesSection.tsx is a 'use client' component with useGSAP + ScrollTrigger.batch
- 6 use case cards in a flat 3-column grid on dark background
- Each card shows icon, persona subtitle (burnt-orange), title, description
- Cards have hover shadow lift via Card hover prop
- Stagger reveal animation fires once on scroll
- Reduced motion users see all cards immediately
- page.tsx cleanly imports both sections, no leftover placeholder markup
- Full build succeeds, page renders correct section flow
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-use-cases/05-02-SUMMARY.md`
</output>
