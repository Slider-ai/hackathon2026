---
phase: 06-final-cta-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/FinalCTA.tsx
  - components/layout/Footer.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Final CTA section displays full-width with compelling headline and subline"
    - "CTA contains email input and Join Waitlist button matching hero form pattern"
    - "Footer displays copyright text, social links, and legal links"
    - "Footer uses dark section styling consistent with site theme"
    - "Page flows from UseCasesSection into FinalCTA into Footer without gaps"
  artifacts:
    - path: "components/sections/FinalCTA.tsx"
      provides: "Final CTA section with waitlist form and scroll animation"
      min_lines: 60
    - path: "components/layout/Footer.tsx"
      provides: "Site footer with copyright, social links, legal links"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "Homepage with all sections including FinalCTA and Footer"
      contains: "FinalCTA"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/FinalCTA.tsx"
      via: "import and JSX render"
      pattern: "import FinalCTA"
    - from: "app/page.tsx"
      to: "components/layout/Footer.tsx"
      via: "import and JSX render"
      pattern: "import Footer"
    - from: "components/sections/FinalCTA.tsx"
      to: "components/ui/Button.tsx"
      via: "import Button for Join Waitlist CTA"
      pattern: "import Button"
---

<objective>
Create the final CTA section with waitlist signup form and site footer, completing the full landing page scroll experience.

Purpose: This is the final phase (6 of 6). The CTA section provides a closing conversion opportunity after the user has scrolled through the full value proposition. The footer gives the page a complete, professional feel with necessary legal and social links.

Output: FinalCTA component (client, with email state + GSAP scroll animation), Footer component (server, dark-themed), and updated page.tsx integrating both.
</objective>

<execution_context>
@C:\Users\antoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\antoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/sections/Hero.tsx
@components/ui/Button.tsx
@components/sections/UseCasesSection.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinalCTA section with waitlist form and scroll animation</name>
  <files>components/sections/FinalCTA.tsx</files>
  <action>
Create a client component (`'use client'`) at `components/sections/FinalCTA.tsx` that implements the closing CTA section.

**Structure:**
- `section` wrapper with `id="cta"`, `ref` for GSAP scope
- Full-width section with centered content, NOT `min-h-screen` (CTA should be impactful but not require a full viewport -- use generous padding instead: `py-24 sm:py-32 lg:py-40`)
- Background: `bg-burnt-orange` for contrast against the stone-950 UseCasesSection above it. This creates a bold, eye-catching closing section in the brand color.
- Text color: `text-white` throughout

**Content:**
- Headline: `text-4xl sm:text-5xl font-bold mb-4` -- "Ready to Build Smarter Presentations?"
- Subline: `text-lg text-white/80 max-w-2xl mx-auto mb-10` -- "Join the waitlist and be first to experience AI-powered presentations inside PowerPoint."
- Email form: Mirror the Hero.tsx waitlist form pattern exactly:
  - `useState` for email
  - `form` with `onSubmit` handler (placeholder -- prevents default, no submission)
  - `flex flex-col sm:flex-row gap-3 max-w-md mx-auto` container
  - `input[type="email"]` with same styling as Hero but adapted for burnt-orange background: `bg-white/10 border border-white/20 text-white placeholder:text-white/50 focus:ring-2 focus:ring-white focus:border-transparent` (adjust from Hero's stone-800/stone-700 to work on burnt-orange bg)
  - Button: use `<Button variant="secondary" size="lg" type="button">Join Waitlist</Button>` -- secondary (teal) variant provides contrast against burnt-orange background (do NOT use primary/burnt-orange on a burnt-orange bg)
  - Reassurance text below: `text-white/60 text-sm mt-3` -- "Free during beta. No credit card required."

**Animation:**
- Use `useGSAP` with `gsap.matchMedia()` following the established pattern from Hero.tsx and UseCasesSection.tsx
- For `(prefers-reduced-motion: no-preference)`: use ScrollTrigger to animate heading, subline, and form fading in from below (`autoAlpha: 0, y: 40`) with stagger. Use `scrollTrigger: { trigger: container, start: 'top 80%', toggleActions: 'play none none none' }` pattern.
- For `(prefers-reduced-motion: reduce)`: `gsap.set` with `clearProps: 'all'` to ensure elements are visible without animation
- Use unique class names for animation targets: `.cta-heading`, `.cta-subline`, `.cta-form` to prevent conflicts with Hero classes
- Import and register ScrollTrigger: `import ScrollTrigger from 'gsap/ScrollTrigger'` and `gsap.registerPlugin(ScrollTrigger)`
- Return `() => mm.revert()` from useGSAP callback for cleanup
  </action>
  <verify>
Run `npx next build` (or `npm run build`) -- no TypeScript or build errors. Verify the file exists and contains: 'use client', useState for email, useGSAP, ScrollTrigger, Button import, form with email input, and matchMedia with reduced motion handling.
  </verify>
  <done>
FinalCTA.tsx exists as a client component with email input + Join Waitlist button (teal variant), scroll-triggered fade-in animation, reduced motion support, and burnt-orange background for contrast.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Footer and integrate both into homepage</name>
  <files>components/layout/Footer.tsx, app/page.tsx</files>
  <action>
**Part A: Create Footer component**

Create a server component (NO 'use client' directive) at `components/layout/Footer.tsx`.

**Structure:**
- `footer` element with `bg-stone-950 text-stone-400 border-t border-stone-800`
- Inner container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12`
- Three-row layout stacked vertically:

Row 1 -- Brand + tagline:
- "Slider" in `text-white font-bold text-lg`
- Short tagline: `text-stone-500 text-sm mt-1` -- "AI-powered presentations, inside PowerPoint."

Row 2 -- Links (flex row, wrapping, with gap):
- Social links (open in new tab with `target="_blank" rel="noopener noreferrer"`):
  - Twitter/X: `href="https://twitter.com"` (placeholder URL)
  - LinkedIn: `href="https://linkedin.com"` (placeholder URL)
  - GitHub: `href="https://github.com"` (placeholder URL)
- Legal links:
  - Privacy Policy: `href="/privacy"` (placeholder, will be 404)
  - Terms of Service: `href="/terms"` (placeholder, will be 404)
- Style links as: `text-stone-500 hover:text-white transition-colors text-sm`
- Separate social and legal groups with a `gap-6` flex, use a divider or just spacing between groups

Row 3 -- Copyright:
- `text-stone-600 text-xs mt-8 pt-8 border-t border-stone-800`
- Text: `(c) ${new Date().getFullYear()} Slider. All rights reserved.` (use the copyright symbol, and `new Date().getFullYear()` for dynamic year)

**No animations needed** -- footer is static content. No GSAP, no client state.

**Part B: Update page.tsx**

Modify `app/page.tsx`:
1. Add import: `import FinalCTA from '@/components/sections/FinalCTA'`
2. Add import: `import Footer from '@/components/layout/Footer'`
3. Remove the existing inline CTA section (the entire `<section id="cta" ...>...</section>` block on lines 22-34 and the `import Button` which is no longer needed)
4. After `<UseCasesSection />`, add `<FinalCTA />`
5. After `<FinalCTA />`, add `<Footer />`
6. Remove the `import Button from '@/components/ui/Button'` line since page.tsx no longer uses Button directly

Final page.tsx should have this structure:
```tsx
import Hero from '@/components/sections/Hero'
import FeatureShowcase from '@/components/sections/FeatureShowcase'
import ProductDemo from '@/components/sections/ProductDemo'
import SkillsSection from '@/components/sections/SkillsSection'
import UseCasesSection from '@/components/sections/UseCasesSection'
import FinalCTA from '@/components/sections/FinalCTA'
import Footer from '@/components/layout/Footer'

export default function Home() {
  return (
    <div>
      <Hero />
      <FeatureShowcase />
      <ProductDemo />
      <SkillsSection />
      <UseCasesSection />
      <FinalCTA />
      <Footer />
    </div>
  )
}
```
  </action>
  <verify>
Run `npx next build` (or `npm run build`) -- no TypeScript or build errors. Verify page.tsx imports FinalCTA and Footer, does NOT import Button directly, and does NOT contain the old inline CTA section. Verify Footer.tsx exists without 'use client' directive and contains copyright, social links (Twitter, LinkedIn, GitHub), and legal links (Privacy, Terms). Run `npm run dev` and confirm the page loads without errors (check terminal output).
  </verify>
  <done>
Footer.tsx exists as a server component with dark theme (stone-950), brand name, social links (Twitter/X, LinkedIn, GitHub), legal links (Privacy, Terms), and dynamic copyright year. page.tsx imports and renders FinalCTA and Footer, replacing the old inline CTA placeholder. The Button import is removed from page.tsx. Full landing page flows: Hero -> Features -> ProductDemo -> Skills -> UseCases -> FinalCTA -> Footer.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` serves the page without runtime errors
3. Page scroll journey is complete: Hero (stone-950) -> FeatureShowcase -> ProductDemo -> Skills (stone-100) -> UseCases (stone-950) -> FinalCTA (burnt-orange) -> Footer (stone-950)
4. FinalCTA has email input + Join Waitlist button that mirrors Hero pattern
5. Footer displays brand name, social links, legal links, and copyright
6. Animations respect prefers-reduced-motion
7. No unused imports in page.tsx
</verification>

<success_criteria>
- CTA-01 satisfied: Full-width section with compelling headline and waitlist signup form
- CTA-02 satisfied: Email input + "Join Waitlist" button (placeholder, mirrors hero CTA pattern)
- FOOT-01 satisfied: Minimal footer with copyright, social links (Twitter/X, LinkedIn, GitHub), and legal links (Privacy, Terms)
- FOOT-02 satisfied: Footer uses dark section styling (stone-950, stone text colors, stone border)
- Landing page is complete end-to-end with no placeholder sections remaining
</success_criteria>

<output>
After completion, create `.planning/phases/06-final-cta-footer/06-01-SUMMARY.md`
</output>
